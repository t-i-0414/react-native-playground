import { Text, View } from 'react-native';

export const SignInScreen = () => (
  <View
    style={{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    }}
  >
    <Text>Sign in</Text>
  </View>
);
